"use client";

const Login = () => {
  const handleLogin = async () => {};

  return (
    <div>
      <button type="button" onClick={handleLogin}>
        Login with Google
      </button>
    </div>
  );
};

export default Login;
